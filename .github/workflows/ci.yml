name: CI

on:
  push:
    branches: [main]
  pull_request:

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  lint:
    name: Lint (php + js)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up PHP
        uses: shivammathur/setup-php@v2
        with:
          php-version: '8.4'
          tools: composer:v2, phpstan

      - name: Install PHP dev deps
        run: composer install --prefer-dist --no-progress

      - name: PHPCS (WordPress-Extra + Docs, PHP 7.4-8.5)
        run: composer lint

      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Install JS dev deps
        run: npm install

      - name: Build
        run: npm run build

      - name: Lint JS
        run: npm run lint-js

  test:
    name: PHPUnit (PHP ${{ matrix.php }}, WP ${{ matrix.wp }})
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        # 7.4 is a deliberate legacy-reach decision (SMB hosts); 8.5 is forward-proofing.
        php: ['7.4', '8.1', '8.4', '8.5']
        wp: ['6.4', 'latest']
        exclude:
          # WP 6.4 predates PHP 8.5; the pairing is untestable.
          - php: '8.5'
            wp: '6.4'
          - php: '7.4'
            wp: 'latest'
    steps:
      - uses: actions/checkout@v4

      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Install JS dev deps + build
        run: npm install && npm run build

      - name: Start wp-env
        env:
          WP_ENV_PHP_VERSION: ${{ matrix.php }}
          WP_ENV_CORE: ${{ matrix.wp == 'latest' && 'WordPress/WordPress' || 'WordPress/WordPress#6.4' }}
        run: npx wp-env start

      - name: Run PHPUnit in wp-env
        env:
          WP_ENV_PHP_VERSION: ${{ matrix.php }}
          WP_ENV_CORE: ${{ matrix.wp == 'latest' && 'WordPress/WordPress' || 'WordPress/WordPress#6.4' }}
        # wp-env is the npm package (there is no official GitHub Action for it); it drives
        # the Docker daemon preinstalled on the runner. start must be explicit: a bare
        # `run` on a fresh runner can find no docker-compose file.
        run: npm run test:php
